# Resilience Copilot runs in deterministic fallback mode by default.
# Optional LLM API keys can be added for future experiments, but are not needed
# for local validation, demo, or the self-built safety benchmark.

RESILIENCE_COPILOT_MODE=fallback
